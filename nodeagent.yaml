# NodeBenchAI application composition. The compiler can now resolve and hash this
# manifest while the existing NodeBench runtime remains authoritative.
schemaVersion: nodeagent.application/v1

application:
  id: nodebench-entity-intelligence
  name: NodeBench Entity Intelligence
  purpose: >-
    Produce source-grounded company, market, and research intelligence through
    the existing durable NodeBench application and its entity-intelligence pack.

authoring:
  directory: ./agent

runtime:
  engine: repo-local-nodebench
  profile: production

provider:
  adapter: repo-local-google-gemini-rest
  model:
    provider: google
    id: gemini-3.5-flash
  secretRef: GEMINI_API_KEY

backend:
  adapter: convex
  config:
    functionsDirectory: ./convex
    layoutStatus: legacy-root

orchestration:
  planner: orchestrator-workers
  implementationSources:
    - ./convex/domains/redesign/chatRuns.ts
    - ./convex/domains/agents
    - ./server/pipeline
  maxConcurrentSubagents: 10
  migrationTarget: nodeagent-native

packs:
  - ./packs/entity-intelligence/pack.yaml

policies:
  directory: ./agent/policies
  enforcement: legacy-code-and-ci

evaluations:
  required:
    - nodebench-conformance
    - nodebench-smoke
    - nodebench-entity-intelligence
    - nodebench-production

contracts:
  event: nodeagent.event/v1
  trace: nodeagent.trace/v1
