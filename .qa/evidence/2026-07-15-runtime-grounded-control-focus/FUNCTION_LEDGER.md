# Runtime-grounded control focus ledger

Run mode: authorized source change and repository release workflow. Production baseline interaction remained read-only.

## Protected contract

- The Agents composer and Send control remain the primary action.
- Plain prompts still open the canonical FastAgent chat path exactly once.
- `/spawn` still reveals its distinct model/agent execution controls.
- Running swarms, human approvals, sources, traces, receipts, exports, failure states, and provenance remain reachable.
- The web navigation remains `Home - Reports - Chat - Inbox - Me`; Workspace remains a separate surface.
- Autonomous maintenance remains available to an authenticated user only after deliberate disclosure.

| id | selector/component | user promise | capability guard | backing field/action/network effect | observed artifact | instance disposition | preserve/reverify assertion |
|---|---|---|---|---|---|---|---|
| A1 | `TopicCanvasPanel` for a signed-out visitor | “Recent topics” are relevant to this visitor | `ORDINARY_CAPABILITY` | `getPublicTaskSessions` returns a global public run feed, not visitor history | Production mobile/desktop renders internal QA prompts such as `TIER_OK` before any visitor intent | REMOVE from signed-out primary view | Authenticated history still queries `getUserTaskSessions`; public trace/feed remains available through its dedicated surface |
| A2 | `TopicCanvasPanel` summary line | `shown / active / sourced` is a decision-useful summary | `ORDINARY_CAPABILITY` | Derived only from the six rendered rows; old pending rows are counted as active | Production shows six months-old pending QA rows as `6 active` | REMOVE | Each retained row keeps its runtime status and trace link |
| A3 | `AutonomousOperationsPanel` on basic Agents view | Operator controls are immediately relevant to every visitor | `PRESERVE_CAPABILITY` | Live Convex health/maintenance queries and authenticated maintenance action | Production exposes the full operator panel and `Run Now` before intent, including to guests | DEFER | `Advanced view` reveals the panel; panel header remains keyboard reachable |
| A4 | Autonomous Operations default-expanded state and header action | Detailed telemetry and maintenance mutation should be exposed immediately | `PRESERVE_CAPABILITY` | Same live queries/action | Production consumes a large passive region and exposes a mutation next to the summary | DEFER | Panel defaults collapsed; expansion reveals details and authenticated action |
| A5 | Operations loading/missing fallbacks | Zeros, “Review”, and “Never” are measured runtime facts | `PRESERVE_CAPABILITY` | Query values are `undefined` until loaded and may legitimately be absent | Production light capture shows `0/0 healthy` while both queries load; source defaults alerts/healing/loops to zero and missing maintenance to `Review` | REPAIR | Loading says `Loading status`; absent measurements say `Not measured`/`No recorded run`; runtime values render unchanged once present |
| A6 | `QuickStatsBar` | Fixed agent count and counterfactual “Cost Saved” are useful operational facts | `ORDINARY_CAPABILITY` | `totalAgents: 6` is hardcoded; savings is modeled against static Haiku prices, not actual saved spend | Source trace in `agentHubQueries.ts`; panel is available in Advanced view | REMOVE | Live running tasks, agent status grid, task history, and actual cost/token provenance remain |
| A7 | `AgentGrid` loading fallback | Every agent is idle with zero tasks before the query resolves | `ORDINARY_CAPABILITY` | `agentStatuses === undefined` is replaced with static `idle / 0 / Never` values | Source trace in `AgentsHub.tsx` | REPAIR | Loading state makes no status assertion; runtime rows render after resolution |
| A8 | `TopicCanvasPanel` row layout | Each trace remains directly inspectable | `PRESERVE_CAPABILITY` | Runtime session id builds canonical trace URL | Mobile rows spend a separate line and large empty area on `Open trace` | COMPACT | Named `Open trace` link remains keyboard reachable in every retained row |
| A9 | `AgentCommandBar`, `ActiveSwarmsSection`, `HumanApprovalQueue` | Ask, observe active work, and review risky work | `PRESERVE_CAPABILITY` | Canonical FastAgent/swarm handlers and live Convex queries | Existing contract tests and production composer | PRESERVE | Existing routing, streaming, approval, and accessibility tests remain green |
| A10 | `UnifiedHubPills` | Reach Workspace/Documents/Schedule/Roadmap | `PRESERVE_CAPABILITY` | Distinct routes; not equivalent to the five-tab web nav | Production responsive rail/select | PRESERVE | Desktop routes and mobile selector remain reachable |
| A11 | Autonomous Operations reads and `Run Now` | Operational telemetry and maintenance belong to the signed-in operator | `PRESERVE_CAPABILITY` | Public queries exposed global health, alerts, and maintenance; mutation trusted client visibility | Independent source audit found guest and non-operator access paths | REPAIR | Server derives role from `adminUsers`; viewers are read-only; non-operators receive no operator data or control |
| A12 | health status and attention summary | Status labels describe the newest measured system state | `PRESERVE_CAPABILITY` | UI severity order and stale threshold diverged from the monitor/cron configuration; count copy truncated the queue | Independent source audit found `0/0`, stale-as-healthy, and incomplete attention claims | REPAIR | Shared monitor configuration drives cron/staleness; unknown and stale are explicit; attention rows are not summarized by a misleading partial count |

## FastAgent and receipt controls

| id | selector/component | user promise | capability guard | backing field/action/network effect | observed artifact | instance disposition | preserve/reverify assertion |
|---|---|---|---|---|---|---|---|
| F1 | signed-out submit path / `demoConversation` | A guest prompt runs the actual agent | `PRESERVE_CAPABILITY` | Real anonymous streaming exists immediately after the old scripted intercept | Every guest prompt was replaced by a canned transcript | REPAIR | Guest sends use the canonical anonymous runtime exactly once; quota and runtime failures remain honest |
| F2 | queue position and wait copy | Queue rank and ETA are runtime measurements | `ORDINARY_CAPABILITY` | Hardcoded `Position 1` and `<5s`; no queue telemetry field | Static status rendered as fact | REMOVE | Actual backend run status remains visible |
| F3 | streaming phase, token-rate, and client cost projections | Progress, throughput, and cost are observed | `ORDINARY_CAPABILITY` | Derived from elapsed time, text, and browser pricing rather than receipts | Projected phases, tokens/second, and dollar values | REMOVE | Exact backend token counts/model provenance remain when supplied |
| F4 | memo/share/deeper duplicate actions | Each control performs a durable named action | `ORDINARY_CAPABILITY` | Blob URLs, local-only memo state, and nonexistent share routes | Controls appeared actionable but did not create durable runtime work | REMOVE or MERGE | Markdown copy/download, notebook save, exports, sources, and real thread actions remain |
| F5 | selection/feedback/bookmark ornament | Controls affect a durable workflow | `ORDINARY_CAPABILITY` | Local-only state or no handler | No-op cards and reaction chrome | REMOVE | Message selection needed for real copy/export remains reachable |
| F6 | receipt fixture fallback | Receipts and approvals are real audit records | `PRESERVE_CAPABILITY` | Demo fixture substituted when live query was empty | Synthetic approvals appeared in operational views | REMOVE | Empty/loading states are honest; owner-scoped live receipts remain |
| F7 | approval decision control | Approving resumes the held action and reports its outcome | `PRESERVE_CAPABILITY` | No execution-resume consumer exists | Mutation previously fabricated success/allowed result | FAIL CLOSED | Decision never claims execution; only pending, owned, escalated receipts may be decided once |
| F8 | guest stop and reload recovery | A guest can recover and cancel the real run they started | `PRESERVE_CAPABILITY` | Anonymous session index and stream cancel mutation | Real guest path exposed auth-only cancel and no thread recovery | REPAIR | Anonymous session ownership gates list/recovery/cancel; cross-session access is denied |
| F9 | persona, custom system prompt, response length | Selected controls change the provider request | `ORDINARY_CAPABILITY` | UI state/localStorage only; no submission or backend field | Labels and success toasts implied runtime configuration | REMOVE | Runtime model selector remains; no unsupported option is shown |
| F10 | reaction, feedback, pin, and bookmark controls | Actions are durable or feed a real evaluation loop | `ORDINARY_CAPABILITY` | Component state/localStorage only | Buttons implied durable organization and RLHF feedback | REMOVE | Real copy, speech, regenerate, and delete actions remain where supported |
| F11 | attachment/video/duplicate voice controls | Media selected in standard chat will be transported | `PRESERVE_CAPABILITY` | Send contract rejects attachments outside authenticated product conversations | Standard chat exposed controls that could only block sending | REPAIR | Attachment/video appears only where transport is supported; one working speech-to-text control remains |
| F12 | import, reply, memory, and branch dialogs | Controls import, reply with context, add runtime memory, or show real branches | `ORDINARY_CAPABILITY` | Empty callback, ignored textarea/state, localStorage, or unrelated thread list | Secondary menus overstated capabilities | REMOVE | Search, timeline navigation, focus/wide layout, and live thread list remain |
| F13 | guest thread delete | A guest can delete a thread owned by their anonymous session | `PRESERVE_CAPABILITY` | Backend was authenticated-only while UI always exposed Delete | Guest control always failed | REPAIR | Anonymous delete requires exact session ownership; cross-session delete is denied |
| F14 | guest model picker | The selected model is the model the anonymous runtime will execute | `PRESERVE_CAPABILITY` | UI offered multiple models while the backend enforced one guest model | Independent source audit found a selectable control with no effect | REPAIR | Guest sees the enforced runtime model as static provenance; only authenticated users receive a working model selector |
| F15 | dropped document analysis | A dropped file is transported and analyzed by the runtime | `PRESERVE_CAPABILITY` | Browser timer changed a local chip from analyzing to ready without upload or analysis | Independent interaction/source audit found a simulated success path | REMOVE | Only real calendar-event drag context is accepted; existing runtime-backed document context remains supported |
| F16 | message view switcher | Answer, Sources, Activity, and Performance are deliberately reachable | `PRESERVE_CAPABILITY` | Desktop alternatives were hidden behind hover-only `More views` behavior | Keyboard and pointer reachability audit | REPAIR | Supported views are direct tabs on desktop and the essential pair remains direct on mobile |
| F17 | anonymous quota reservation | A rejected cross-session request cannot consume another caller's quota | `PRESERVE_CAPABILITY` | Reservation occurred before target-thread ownership resolution | Independent backend audit | REPAIR | Ownership resolves before quota mutation; wrong-session attempts leave quota untouched |
| F18 | `@web`, `@docs`, `@code`, `@data`, `@deep` suggestions | Selecting a mention changes runtime routing | `ORDINARY_CAPABILITY` | No backend parser or send-contract field consumed these tokens | Source audit found decorative completions | REMOVE | `/spawn` remains because it has a real parser and distinct runtime path |

## Exact cockpit surfaces

| id | selector/component | user promise | capability guard | backing field/action/network effect | observed artifact | instance disposition | preserve/reverify assertion |
|---|---|---|---|---|---|---|---|
| E1 | report list/detail fallback | Cards and dossier content are saved runtime reports | `PRESERVE_CAPABILITY` | Runtime report query could be empty or an id could be unknown | Empty/unknown states fell back to named fixture companies | REMOVE | Loading, empty, and not-found states are explicit; live reports retain sources/provenance |
| E2 | mobile chat body | Mobile chat runs the same backend as desktop | `PRESERVE_CAPABILITY` | Static verified answer and inert composer lived in a separate mobile tree | Mobile implied a completed sourced run without a send | REPAIR | Mobile mounts the canonical runtime-backed chat inside the mobile surface contract |
| E3 | inbox seed | Inbox items are current runtime nudges | `PRESERVE_CAPABILITY` | `INBOX_SEED` substituted when query returned no rows | Synthetic actionable alerts appeared in an empty inbox | REMOVE | Live nudges remain actionable; loading/empty/filter-empty states are distinct |
| E4 | profile, plan, usage, and notebook seed | Identity, entitlement, usage, and saved work are account facts | `PRESERVE_CAPABILITY` | Hardcoded person/company/PRO and notebook/report counts | Static account claims appeared as live | REMOVE | Auth identity and runtime entities/sessions render when present; guest/missing data is labeled |
| E5 | pulse and vanity metrics | Home metrics measure recorded work | `PRESERVE_CAPABILITY` | Large static counts and seeded spark lines | Projected operational scale appeared as telemetry | REMOVE | Pulse is absent unless the runtime ledger reports live metrics; only returned values render |
| E6 | responsive surface split | Mobile keeps usable nav, safe areas, and the same core capability | `PRESERVE_CAPABILITY` | Fixture-heavy mobile-only bodies diverged from desktop runtime | Two semantic implementations drifted | MERGE | One canonical live child tree renders in desktop/mobile wrappers; five-tab mobile contract remains |
| E7 | `ExactWorkspaceKitPage` and `WORKSPACE_*` graph | Exported workspace UI represents the deployed Workspace runtime | `ORDINARY_CAPABILITY` | No production import; `/workspace` resolves to `UniversalWorkspacePage` | Thousands of dead fixture lines described companies, sources, claims, and metrics that were never the live route | REMOVE | Deployed Workspace remains owned by `UniversalWorkspacePage`; the separately deployed developer surface remains reachable |
| E8 | Home starter prompts and proof pills | Examples and promises describe capabilities available from the current surface | `PRESERVE_CAPABILITY` | Named-company/attached-file fixtures and broad export promises were static copy | Browser audit found DISCO, an unattached 10-K, Mercor, and unsupported Notes/Notion/Linear/CSV claims | REPAIR | Generic prompts invoke the real pipeline; one concise proof line states only the server-run and saved-report behavior backed by runtime |
| E9 | Chat starter rows and secondary actions | Each visible action has one clear owner and a real effect | `PRESERVE_CAPABILITY` | Initial-turn suggestions were duplicated; save/share/deeper/confirm/entity-pill branches were duplicate, inert, or unsupported | Source and local browser audit | REMOVE or MERGE | Composer owns the single starter row; runtime answer, sources, activity, exports, and follow-up send remain |
| E10 | Inbox `Snooze 24h` | The row disappears only after the owned backend mutation succeeds | `PRESERVE_CAPABILITY` | Optimistic removal did not require `{ok:true}` and had no visible failure | Interaction/source audit | REPAIR | Await the owner-scoped mutation; keep the row and render an alert on failure |
| E11 | `/developers` MCP terminal transcript | Terminal output reflects an invocation the caller actually made | `PRESERVE_CAPABILITY` | Reachable page rendered a canned DISCO tool call, result, latency, and token count | Route reachability trace and Chromium audit | REMOVE | Developer page starts in an honest pre-call state and retains its real connection/invocation surface |

## Pipeline ownership and runtime controls

| id | selector/component | user promise | capability guard | backing field/action/network effect | observed artifact | instance disposition | preserve/reverify assertion |
|---|---|---|---|---|---|---|---|
| P1 | pipeline launch mutations | A run starts inside the current account or anonymous session | `PRESERVE_CAPABILITY` | Public mutations accepted a caller-supplied `ownerKey` | Independent source audit found owner impersonation on run creation | REPAIR | Public launch derives `user:<id>` server-side or an anonymous-session owner from the session possession credential; trusted worker/MCP entrypoints remain internal |
| P2 | run history, detail, bundle, stats, and stream | Activity and partial output belong to the current caller | `PRESERVE_CAPABILITY` | Public reads accepted arbitrary owner filters or returned global rows by default | Independent source audit found cross-tenant run content and streaming text exposure | REPAIR | Every public read resolves the caller owner and fails closed on mismatched run ids |
| P3 | schedule list/create/toggle/delete | Automatic refreshes are owned controls | `PRESERVE_CAPABILITY` | Public CRUD trusted owner keys and ids from the browser | Independent source audit found cross-tenant schedule reads and writes | REPAIR | Create derives ownership; list is owner-indexed; toggle/delete verify row ownership before mutation |
| P4 | pipeline evaluation scorecard | Scorecard measures only the caller's visible runs | `PRESERVE_CAPABILITY` | UI aggregate queried global recent pipeline runs | Source audit found a global operational projection in the personal cockpit | REPAIR or DEFER | Scope the aggregate to the resolved caller or hide it until an owner-scoped contract exists |
| P5 | PipelineLauncher, Activity, and schedule panels | Browser inputs describe intent, not authority | `PRESERVE_CAPABILITY` | UI constructed `user:` / `session:` owner keys and passed them to public endpoints | Source trace across Exact Home and pipeline panels | REPAIR | Browser passes only `anonymousSessionId` when needed; authenticated authority always comes from server auth |
